async_pool {
  max_queue_size = 8 // Maximum number of items queued
  max_objects = 8 // Maximum number of connections to DB
  validation_interval = 3000
  max_idle = 1000
}

postgres {
  username = "dbo"
  password = "password"
  database = "yabba"
  host = "192.168.59.103"
  port = 9999
}